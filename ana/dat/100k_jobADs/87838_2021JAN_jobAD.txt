"Használt vitorlák árbócok eladók | Hajóbazár.hu - Hajós apróhirdetésekBejelentkezésRegisztrációVitorlás hajókMotoros hajókCsónakok, EgyébSzerviz, tárolás, sportAdjon fel Hirdetést!Használt vitorlák árbócok eladók IngyenesA képeken látható használt vitorlák árbócok eladók Siófokon.Ár megegyezés szerint.Kapcsolódó HirdetésekKapcsolatfelvételcsukiliKérem írja be, mennyi 5+3=? (Humán teszt)Kategória Vitorla, árboc, veret, Hozzáadva aug 31, 2020 Nézettség 449LegnépszerűbbVitorlás veretek: Bimini top,Kiülők ,Orr kilépő 85000hatméteres alu árboc 60 000 Ftgénua vitorla fokrollerrel 65000Vitorlázat 280000Furlex 200 TD+Furlex 200TDE+vezérlőegység 5000€Legfrissebb hirdetésekAKCIÓ! Új GARMIN GPSMAP 922 az új ár -40% eladó!Budapesten új horgászcsónak eladóMEDENCÉK GYÁRTÁSASUP deszkákMONTERY 194FS 4.3mpi, szép állapotKapcsolatCím:1107 Budapest Gém utca 6.Telefonszám:+36-70-421-9189Központi e-mail címünk: [email protected]ÜzemeltetőCégteszt Network Kft.Turi AttilaAdószám:24268309-2-42Cégjegyzég szám:01-09-998850HirdetésHajobazar.hu A hajós közösség apróhirdetési portálja.	Felhasználói FeltételekFiókomKapcsolatfelvételMédiaajánlatRegisztrációMűveletekMenü	Kiválasztott médiaművelet	<# } #><# } #><# if ( data.message ) { #>{{ data.message }}<# } #>Húzzunk ide fájlt a feltöltéshezvagyFájlok kiválasztásaA maximálisan feltölthető fájlméret: 32 MB.	<# if ( data.suggestedWidth && data.suggestedHeight ) { #>Ajánlott képméret: {{data.suggestedWidth}} x {{data.suggestedHeight}} pixel.	<# } #>Rács nézetHibák átugrása/ –{{ data.message }}<# if ( ! data.hasNext ) { #> disabled<# } #>>Következő média fájl szerkesztésePárbeszéd bezárása<# if ( data.uploading ) { #><# } else if ( data.sizes && data.sizes.large ) { #> <# } else if ( data.sizes && data.sizes.full ) { #> <# } else if ( -1 === jQuery.inArray( data.type, [ 'audio', 'video' ] ) ) { #> <# } #> <# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } #><# if ( 'image' === data.type && ! data.uploading && data.sizes && data.can.save ) { #>Kép szerkesztése<# } else if ( 'pdf' === data.subtype && data.sizes ) { #>Dokumentum előnézet<# } #>Elmentve.RészletekFeltöltve: {{ data.dateFormatted }}Feltöltő: <# if ( data.authorLink ) { #> {{ data.authorName }} <# } else { #> {{ data.authorName }} <# } #><# if ( data.uploadedToTitle ) { #>Feltöltési hely: <# if ( data.uploadedToLink ) { #> {{ data.uploadedToTitle }} <# } else { #> {{ data.uploadedToTitle }} <# } #><# } #>Fájlnév: {{ data.filename }}Fájl típusa: {{ data.mime }}Fájlméret: {{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>Méretek: {{ data.width }} x {{ data.height }} pixel	<# } #> <# if ( data.originalImageURL && data.originalImageName ) { #> Eredeti kép:	{{data.originalImageName}} <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Hossz: {{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( 'audio' === data.type && data.meta.bitrate ) { #>Bitráta: {{ Math.round( data.meta.bitrate / 1000 ) }}kb/s <# if ( data.meta.bitrate_mode ) { #> {{ ' ' + data.meta.bitrate_mode.toUpperCase() }} <# } #><# } #> <# if ( data.mediaStates ) { #>Használatban, mint: {{ data.mediaStates }}<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Helyettesítő szövegIsmertessük a kép célját (megnyitás új fülön). Hagyjuk üresen, ha a kép magáért beszél.<# } #> Cím <# if ( 'audio' === data.type ) { #> Előadó Album <# } #> Felirat{{ data.caption }}Leírás{{ data.description }}Fájl URL:URL másolása a vágólapraMásolva!<# if ( data.link ) { #> Csatolmány oldal <# } #> <# if ( data.can.save ) { #> <# if ( data.link ) { #> | <# } #> További részletek szerkesztése <# } #> <# if ( ! data.uploading && data.can.remove ) { #> <# if ( data.link || data.can.save ) { #> | <# } #>Végleges törlés<# } #><# } else if ( 'image' === data.type && data.size && data.size.url ) { #><# } else { #><# if ( data.image && data.image.src && data.image.src !== data.icon ) { #> <# } else if ( data.sizes && data.sizes.medium ) { #> <# } else { #> <# } #>{{ data.filename }}<# } #><# if ( data.buttons.close ) { #>Eltávolítás<# } #><# if ( data.buttons.check ) { #>Megszüntet<# } #> <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; if ( data.describe ) { if ( 'image' === data.type ) { #> <# } else { #> <# if ( 'video' === data.type ) { #> aria-label=\"Videó címsor\" placeholder=\"Videó címsor…\" <# } else if ( 'audio' === data.type ) { #> aria-label=\"Hang címsor\" placeholder=\"Hang címsor…\" <# } else { #> aria-label=\"Média címsor\" placeholder=\"Média címsor…\" <# } #> {{ maybeReadOnly }} /> <# } } #> Elmentve.<# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } else { #><# if ( data.uploading ) { #><# } else if ( 'image' === data.type && data.size && data.size.url ) { #> <# } else { #> <# } #><# } #>{{ data.filename }}{{ data.dateFormatted }}{{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>{{ data.width }} x {{ data.height }} pixel	<# } #> <# if ( data.originalImageURL && data.originalImageName ) { #> Eredeti kép:	{{data.originalImageName}} <# } #> <# if ( data.can.save && data.sizes ) { #> Kép szerkesztése <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Hossz:	{{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( data.mediaStates ) { #>Használatban, mint: {{ data.mediaStates }}<# } #> <# if ( ! data.uploading && data.can.remove ) { #>Végleges törlés<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Alternatív szövegIsmertessük a kép célját (megnyitás új fülön). Hagyjuk üresen, ha a kép magáért beszél.<# } #> Cím <# if ( 'audio' === data.type ) { #> Előadó Album <# } #> Felirat{{ data.caption }}Leírás{{ data.description }}Fájl URL:URL másolása a vágólapraMásolva!<# if ( data.editable ) { #>Kiválasztás szerkesztése<# } #> <# if ( data.clearable ) { #>Kiürítés<# } #><# if ( 'image' === data.type ) { #> Illesztés <# if ( data.userSettings ) { #> data-user-setting=\"align\" <# } #>> Bal	Közép	Jobb	Egyik sem	 <# } #> <# if ( data.model.canEmbed ) { #> Beágyazás vagy hivatkozás	<# } else { #> Hivatkozás	<# } #> <# if ( data.userSettings && ! data.model.canEmbed ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( data.model.canEmbed ) { #> Médialejátszó beágyazása	<# } else { #> Egyik sem	<# } #> <# if ( data.model.canEmbed ) { #> Hivatkozás a média fájlhoz	<# } else { #> Média fájl	<# } #> <# if ( data.model.canEmbed ) { #> Hivatkozás a csatolmány oldalhoz	<# } else { #> Csatolmány oldal	<# } #> <# if ( 'image' === data.type ) { #> Egyéni URL	<# } #> URL <# if ( 'undefined' !== typeof data.sizes ) { #> Méret <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Bélyegkép – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Közepes – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Nagy – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Teljes méret – {{ size.width }} × {{ size.height }} <# } #> <# } #>Hivatkozás <# if ( data.userSettings ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( ! wp.media.galleryDefaults.link || 'post' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Csatolmány oldal	<# if ( 'file' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Média fájl	<# if ( 'none' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Egyik sem	 Oszlopok <# if ( 1 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 1	<# if ( 2 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 2	<# if ( 3 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 3	<# if ( 4 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 4	<# if ( 5 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 5	<# if ( 6 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 6	<# if ( 7 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 7	<# if ( 8 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 8	<# if ( 9 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 9	 Véletlen sorrend Méret <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #> > Bélyegkép	Közepes	Nagy	Teljes méret	<# var emptyModel = _.isEmpty( data.model ), isVideo = 'video' === data.controller.get('library').props.get('type'); #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> <# if ( isVideo ) { #> Videólista mutatása	<# } else { #> Mutassuk a lejátszó listát	<# } #> <# if ( ! isVideo ) { #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Mutassuk az előadó nevét a lejátszási listában	 <# } #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Képek mutatása	Helyettesítő szövegIsmertessük a kép célját (megnyitás új fülön). Hagyjuk üresen, ha a kép magáért beszél.FeliratIgazítBal	Közép	Jobb	Egyik sem	HivatkozásKép URL	Egyéni URL	Egyik sem	URLIsmertessük a kép célját (megnyitás új fülön). Hagyjuk üresen, ha a kép magáért beszél.Felirat{{ data.model.caption }}Képernyő beállításaiIgazítBal	Közép	Jobb	Egyik sem	<# if ( data.attachment ) { #> <# if ( 'undefined' !== typeof data.attachment.sizes ) { #> Méret <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Bélyegkép – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Közepes – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Nagy – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Teljes méret – {{ size.width }} × {{ size.height }} <# } #> Egyéni méret	 <# } #>Szélesség × MagasságKépméret pixelben<# } #> Hivatkozás <# if ( data.attachment ) { #> Média fájl	Csatolmány oldal	<# } else { #> Kép URL	<# } #> Egyéni URL	Egyik sem	 URLHaladó lehetőségekKép cím tulajdonságok Kép CSS osztály<# if ( data.model.linkTargetBlank ) { #>checked=\"checked\"<# } #>> Hivatkozás megnyitása új ablakban Kapcsolódó hivatkozás CSS osztály hivatkozás<# if ( data.attachment && window.imageEdit ) { #><# } #><# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> URLAudió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.mp3 ) ) { if ( ! _.isUndefined( html5types.mp3 ) ) { delete html5types.mp3; } #> MP3Audió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.ogg ) ) { if ( ! _.isUndefined( html5types.ogg ) ) { delete html5types.ogg; } #> OGGAudió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.flac ) ) { if ( ! _.isUndefined( html5types.flac ) ) { delete html5types.flac; } #> FLACAudió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.m4a ) ) { if ( ! _.isUndefined( html5types.m4a ) ) { delete html5types.m4a; } #> M4AAudió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.wav ) ) { if ( ! _.isUndefined( html5types.wav ) ) { delete html5types.wav; } #> WAVAudió forrás eltávolítása<# } #> <# if ( ! _.isEmpty( html5types ) ) { #>Adjunk hozzá alternatív forrást a HTML5 lejátszáshoz: <# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #>ElőtöltésAutomatikusMeta adatEgyik semAutomatikus lejátszás Loop<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> URLVideó forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.mp4 ) ) { if ( ! _.isUndefined( html5types.mp4 ) ) { delete html5types.mp4; } #> MP4Videó forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.m4v ) ) { if ( ! _.isUndefined( html5types.m4v ) ) { delete html5types.m4v; } #> M4VVideó forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.webm ) ) { if ( ! _.isUndefined( html5types.webm ) ) { delete html5types.webm; } #> WEBMVideó forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.ogv ) ) { if ( ! _.isUndefined( html5types.ogv ) ) { delete html5types.ogv; } #> OGVVideó forrás eltávolítása<# } #> <# if ( ! _.isEmpty( data.model.flv ) ) { if ( ! _.isUndefined( html5types.flv ) ) { delete html5types.flv; } #> FLVVideó forrás eltávolítása<# } #><# if ( ! _.isEmpty( html5types ) ) { #>Adjunk hozzá alternatív forrást a HTML5 lejátszáshoz: <# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #> <# if ( ! _.isEmpty( data.model.poster ) ) { #> PoszterképPlakátkép eltávolítása<# } #>ElőtöltésAutomatikusMeta adatEgyik semAutomatikus lejátszás Loop <# var content = ''; if ( ! _.isEmpty( data.model.content ) ) { var tracks = jQuery( data.model.content ).filter( 'track' ); _.each( tracks.toArray(), function( track, index ) { content += track.outerHTML; #> Sávok (feliratok, képaláírások, leírások, fejezetek, vagy meta adatok) A srclang, label, és kind értékek szerkesztésével beállítható a videósáv nyelve és megjelenése.	Videótrack eltávolítása<# } ); #> <# } else { #> Sávok (feliratok, képaláírások, leírások, fejezetek, vagy meta adatok)Nincsenek hozzárendelt feliratok. <# } #>{{ content }}<# if ( attachment.caption ) { #>{{{ data.verifyHTML( attachment.caption ) }}}<# } #><# if ( index % data.columns === data.columns - 1 ) { #><# } #> <# } ); #><# } else { #>Nem találhatóak elemek.<# } #>Böngészőikonként<# print( 'Hajóbazár.hu – Hajós apróhirdetések' ) #>AlkalmazásikonkéntKedves Látogató! Tájékoztatjuk, hogy a honlap felhasználói élmény fokozásának érdekében sütiket alkalmazunk. A honlapunk használatával ön a tájékoztatásunkat tudomásul veszi.Elfogadom"
