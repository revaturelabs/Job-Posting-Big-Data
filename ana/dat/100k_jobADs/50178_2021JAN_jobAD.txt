"Portal del Empleo - Portal de Empleo de la Fundación GoodJob Portal del Empleo – Portal de Empleo de la Fundación GoodJobREGISTRARSE ACCEDERToggle navigationHomeOferta de EmpleoLista de EmpleosPublicar una Oferta de EmpleoContactoBusco trabajoLa mejor manera de encontrar empleoEncuentra trabajoOfrezco trabajoEncuentra los profesionales que necesitasPublica una ofertaEncontrar un empleoBuscarUbicaciónCategoríaSeleccione Categoria de EmpleoCalidad, producción e I+DComercial y ventasProfesiones, artes y oficiosSanidad y saludModalidadSeleccione Tipo de EmpleoJornada CompletaMedia JornadaTemporalExperiencia (-/+) - 12345678910	 12345678910	Salario (€) / por año 10K20K50K75K100K150K200K250K300K400K500K	 10K20K50K75K100K150K200K250K300K400K500K	BuscarBúsqueda AvanzadaEl Camino Más Fácil	Primer PasoRegístrateSegundo PasoCrea tu perfilTercer PasoSube tu curriculumCuarto PasoBusca entre las ofertas y envía tu CVGuía paso a paso para Solicitantes de empleoSobre la FundaciónLa Fundación Goodjob nace para fomentar el incremento de las competencias personales y profesionales, así como la inserción laboral, de personas con discapacidad y, en general, en riesgo de exclusión social.Enlaces de interésFundación GoodJobPolítica de PrivacidadPolítica de calidadÚltimos Twitsabout 3 días agoBuscamos un/a responsable de desarrollo de negocio y proyectos para nuestras oficinas en 📍 Barcelona. Si tienes exp… https://t.co/uTBg4XTtqhabout 4 días agoArranca el proceso de selección para la 3ª edición del Programa #include en Madrid, Barcelona, León, Valladolid y V… https://t.co/msUXrGNoWLabout 1 semana agoSi te perdiste el programa de @MadridTrabajaOM, dale al ▶️y no te pierdas a @cesaracimut. ¿Quieres saber cuándo se… https://t.co/ROPAA6wrXK2015 © Fundación GoodJob.	Uso de cookiesEste sitio web utiliza cookies para que usted tenga la mejor experiencia de usuario. Si continúa navegando está dando su consentimiento para la aceptación de las mencionadas cookies y la aceptación de nuestra política de cookies, pinche el enlace para mayor información. ACEPTARAviso de cookies<# } #><# } #><# if ( data.message ) { #>{{ data.message }}<# } #>Arrastra archivos a cualquier lugar para subirlosoSeleccionar archivosTamaño máximo de archivo: 2 MB.	<# if ( data.suggestedWidth && data.suggestedHeight ) { #>Dimensiones de imagen sugeridas: {{data.suggestedWidth}} por {{data.suggestedHeight}} píxeles.	<# } #>Vista de cuadrículaDescartar errores/ –{{ data.message }}Editar el siguiente medioCerrar diálogo<# } else if ( data.sizes && data.sizes.large ) { #> <# } else if ( data.sizes && data.sizes.full ) { #> <# } else if ( -1 === jQuery.inArray( data.type, [ 'audio', 'video' ] ) ) { #> <# } #> <# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } #><# if ( 'image' === data.type && ! data.uploading && data.sizes && data.can.save ) { #>Editar imagen<# } else if ( 'pdf' === data.subtype && data.sizes ) { #> Vista previa del documento	<# } #>Guardado.Nombre de archivo: {{ data.filename }}Tipo de archivo: {{ data.mime }}Subido el: {{ data.dateFormatted }}Tamaño de archivo: {{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>Dimensiones: {{ data.width }} por {{ data.height }} píxeles	<# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Longitud: {{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( 'audio' === data.type && data.meta.bitrate ) { #>Bitrate: {{ Math.round( data.meta.bitrate / 1000 ) }}kb/s <# if ( data.meta.bitrate_mode ) { #> {{ ' ' + data.meta.bitrate_mode.toUpperCase() }} <# } #><# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Texto alternativoDescribe el propósito de la imagen (abre en una nueva pestaña). Déjalo en blanco si la imagen es meramente decorativa.<# } #> Título <# if ( 'audio' === data.type ) { #> Artista Álbum <# } #> Leyenda{{ data.caption }}Descripción{{ data.description }}Subido por {{ data.authorName }}<# if ( data.uploadedToTitle ) { #>Subido a <# if ( data.uploadedToLink ) { #> {{ data.uploadedToTitle }} <# } else { #> {{ data.uploadedToTitle }} <# } #><# } #> Copiar enlaceVer página de adjuntos <# if ( data.can.save ) { #> | Editar más detalles <# } #> <# if ( ! data.uploading && data.can.remove ) { #> |Borrar permanentemente<# } #><# } else if ( 'image' === data.type && data.sizes ) { #><# } else { #><# if ( data.image && data.image.src && data.image.src !== data.icon ) { #> <# } else if ( data.sizes && data.sizes.medium ) { #> <# } else { #> <# } #>{{ data.filename }}<# } #><# if ( data.buttons.close ) { #>Quitar<# } #><# if ( data.buttons.check ) { #>Anular selección<# } #> <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; if ( data.describe ) { if ( 'image' === data.type ) { #> <# } else { #> <# if ( 'video' === data.type ) { #> placeholder=\"Describe este video…\" <# } else if ( 'audio' === data.type ) { #> placeholder=\"Describe este archivo de audio…\" <# } else { #> placeholder=\"Describe este archivo de medios…\" <# } #> {{ maybeReadOnly }} /> <# } } #> Guardado.<# if ( data.uploading ) { #><# } else if ( 'image' === data.type && data.sizes ) { #> <# } else { #> <# } #>{{ data.filename }}{{ data.dateFormatted }}{{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>{{ data.width }} por {{ data.height }} píxeles	<# } #> <# if ( data.can.save && data.sizes ) { #> Editar imagen <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Longitud:	{{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( ! data.uploading && data.can.remove ) { #>Borrar permanentemente<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Texto alternativoDescribe el propósito de la imagen (abre en una nueva pestaña). Déjalo en blanco si la imagen es meramente decorativa.<# } #> Título <# if ( 'audio' === data.type ) { #> Artista Álbum <# } #> Leyenda{{ data.caption }}Descripción{{ data.description }}Copiar enlace <# if ( data.editable ) { #>Editar selección<# } #> <# if ( data.clearable ) { #>Borrar<# } #><# if ( 'image' === data.type ) { #> Alineación <# if ( data.userSettings ) { #> data-user-setting=\"align\" <# } #>> Izquierda	Centrar	Derecha	Ninguna	 <# } #><# if ( data.model.canEmbed ) { #> Incrustar o enlazar <# } else { #> Enlazado a <# } #> <# if ( data.userSettings && ! data.model.canEmbed ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( data.model.canEmbed ) { #> Incrustar reproductor de medios	<# } else { #> Ninguna	<# } #> <# if ( data.model.canEmbed ) { #> Enlace al archivo de medios	<# } else { #> Archivo multimedia	<# } #> <# if ( data.model.canEmbed ) { #> Enlace a página de adjuntos	<# } else { #> Página de adjuntos	<# } #> <# if ( 'image' === data.type ) { #> URL personalizada	<# } #><# if ( 'undefined' !== typeof data.sizes ) { #> Tamaño <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Miniatura – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Medio – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Grande – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Tamaño completo – {{ size.width }} × {{ size.height }} <# } #> <# } #>Enlazado a <# if ( data.userSettings ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( ! wp.media.galleryDefaults.link || 'post' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Página de adjuntos	<# if ( 'file' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Archivo multimedia	<# if ( 'none' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Ninguna	 Columnas <# if ( 1 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 1	<# if ( 2 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 2	<# if ( 3 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 3	<# if ( 4 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 4	<# if ( 5 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 5	<# if ( 6 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 6	<# if ( 7 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 7	<# if ( 8 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 8	<# if ( 9 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 9	 Orden aleatorio Tamaño <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #> > Miniatura	Medio	Grande	Tamaño completo	<# var emptyModel = _.isEmpty( data.model ), isVideo = 'video' === data.controller.get('library').props.get('type'); #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> <# if ( isVideo ) { #> Mostrar lista de vídeos <# } else { #> Mostrar lista de reproducción <# } #> <# if ( ! isVideo ) { #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Mostrar nombre de artista en la lista de pistas <# } #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Mostrar imágenesTexto alternativoDescribe el propósito de la imagen (abre en una nueva pestaña). Déjalo en blanco si la imagen es meramente decorativa.LeyendaAlineaciónIzquierda	Centrar	Derecha	Ninguna	Enlazado aURL de la imagen	URL personalizada	Ninguna	<# } #>Texto alternativoDescribe el propósito de la imagen (abre en una nueva pestaña). Déjalo en blanco si la imagen es meramente decorativa.Leyenda{{ data.model.caption }}Ajustes de visualizaciónAlineaciónIzquierda	Centrar	Derecha	Ninguna	<# if ( data.attachment ) { #> <# if ( 'undefined' !== typeof data.attachment.sizes ) { #> Tamaño <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Miniatura – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Medio – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Grande – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Tamaño completo – {{ size.width }} × {{ size.height }} <# } #> Tamaño personalizado	 <# } #>Ancho (px) × Altura (px)<# } #>Enlazado a <# if ( data.attachment ) { #> Archivo multimedia	Página de adjuntos	<# } else { #> URL de la imagen	<# } #> URL personalizada	Ninguna	Opciones AvanzadasAtributo «title» de la imagen Clases CSS de la imagen<# if ( data.model.linkTargetBlank ) { #>checked=\"checked\"<# } #>>Abrir enlace en una pestaña nuevaRelación del enlace Clase CSS del enlace<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #>URLQuitar fuente de audio<# } #> <# if ( ! _.isEmpty( data.model.mp3 ) ) { if ( ! _.isUndefined( html5types.mp3 ) ) { delete html5types.mp3; } #>MP3Quitar fuente de audio<# } #> <# if ( ! _.isEmpty( data.model.ogg ) ) { if ( ! _.isUndefined( html5types.ogg ) ) { delete html5types.ogg; } #>OGGQuitar fuente de audio<# } #> <# if ( ! _.isEmpty( data.model.flac ) ) { if ( ! _.isUndefined( html5types.flac ) ) { delete html5types.flac; } #>FLACQuitar fuente de audio<# } #> <# if ( ! _.isEmpty( data.model.m4a ) ) { if ( ! _.isUndefined( html5types.m4a ) ) { delete html5types.m4a; } #>M4AQuitar fuente de audio<# } #> <# if ( ! _.isEmpty( data.model.wav ) ) { if ( ! _.isUndefined( html5types.wav ) ) { delete html5types.wav; } #>WAVQuitar fuente de audio<# } #> <# if ( ! _.isEmpty( html5types ) ) { #>Añadir fuentes alternativas para mejorar la reproducción en HTML5:<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #>PrecargaAutomáticoMetadatosNingunaReproducción automática Repetir<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #>URLQuitar fuente de vídeo<# } #> <# if ( ! _.isEmpty( data.model.mp4 ) ) { if ( ! _.isUndefined( html5types.mp4 ) ) { delete html5types.mp4; } #>MP4Quitar fuente de vídeo<# } #> <# if ( ! _.isEmpty( data.model.m4v ) ) { if ( ! _.isUndefined( html5types.m4v ) ) { delete html5types.m4v; } #>M4VQuitar fuente de vídeo<# } #> <# if ( ! _.isEmpty( data.model.webm ) ) { if ( ! _.isUndefined( html5types.webm ) ) { delete html5types.webm; } #>WEBMQuitar fuente de vídeo<# } #> <# if ( ! _.isEmpty( data.model.ogv ) ) { if ( ! _.isUndefined( html5types.ogv ) ) { delete html5types.ogv; } #>OGVQuitar fuente de vídeo<# } #> <# if ( ! _.isEmpty( data.model.flv ) ) { if ( ! _.isUndefined( html5types.flv ) ) { delete html5types.flv; } #>FLVQuitar fuente de vídeo<# } #><# if ( ! _.isEmpty( html5types ) ) { #>Añadir fuentes alternativas para mejorar la reproducción en HTML5:<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #> <# if ( ! _.isEmpty( data.model.poster ) ) { #>Imagen de posterQuitar imagen del póster<# } #>PrecargaAutomáticoMetadatosNingunaReproducción automática Repetir<# var content = ''; if ( ! _.isEmpty( data.model.content ) ) { var tracks = jQuery( data.model.content ).filter( 'track' ); _.each( tracks.toArray(), function (track) { content += track.outerHTML; #> Pistas (subtítulos, leyendas, descripciones, capítulos o metadatos)Quitar pista de vídeo<# } ); #> <# } else { #> Pistas (subtítulos, leyendas, descripciones, capítulos o metadatos) No hay subtítulos asociados. <# } #>{{ content }}<# if ( attachment.caption ) { #>{{{ data.verifyHTML( attachment.caption ) }}}<# } #><# if ( index % data.columns === data.columns - 1 ) { #><# } #> <# } ); #><# } else { #>No se han encontrado elementos.<# } #>Como icono del navegadorPortal del EmpleoComo icono de aplicación"
