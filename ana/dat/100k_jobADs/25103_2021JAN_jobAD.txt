"Login – job-partnersSkip to contentMenuHomeHledate prace ?Službypro FirmyJob AgencyContactPrivacy PolicyPravidla a Politika.for manegersLoginMy Profile0.00€ 0 itemsjob-partnersrecruitment agencyLoginUsername*Password*Remember MeLost your password?Note: It looks like JavaScript is disabled in your browser. Some elements of this form may require JavaScript to work properly. If you have trouble submitting the form, try enabling JavaScript momentarily and resubmit. JavaScript settings are usually found in Browser Settings or Browser Developer menu.24/01/2019 licznik odwiedzin joomlaProudly powered by WordPress | Theme: Lodestar by Automattic.	Czech	Polish	 Czech	<# } #><# } #><# if ( data.message ) { #>{{ data.message }}<# } #>Přetáhnout soubory z počítačeneboVybrat souboryMaximální velikost nahrávaného souboru: 256 MB.	<# if ( data.suggestedWidth && data.suggestedHeight ) { #>Doporučené rozměry obrázku:{{data.suggestedWidth}} na {{data.suggestedHeight}} pixelů.	<# } #>Pravidelná mřížkaSkrýt chyby/ –{{ data.message }}Upravit následující mediální souborZavřít dialogové okno<# } else if ( data.sizes && data.sizes.large ) { #> <# } else if ( data.sizes && data.sizes.full ) { #> <# } else if ( -1 === jQuery.inArray( data.type, [ 'audio', 'video' ] ) ) { #> <# } #> <# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } #><# if ( 'image' === data.type && ! data.uploading && data.sizes && data.can.save ) { #>Upravit obrázek<# } else if ( 'pdf' === data.subtype && data.sizes ) { #> Náhled dokumentu	<# } #>Uloženo.Název souboru: {{ data.filename }}Typ souboru: {{ data.mime }}Nahráno: {{ data.dateFormatted }}Velikost souboru: {{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>Rozměry: {{ data.width }} x {{ data.height }} px	<# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Délka: {{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( 'audio' === data.type && data.meta.bitrate ) { #>Přenosová rychlost: {{ Math.round( data.meta.bitrate / 1000 ) }}kb/s <# if ( data.meta.bitrate_mode ) { #> {{ ' ' + data.meta.bitrate_mode.toUpperCase() }} <# } #><# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Alternativní textPopište smysl obrázku (otevře se na nové záložce). Nevyplňujte jestli je obrázek čistě dekorativní.<# } #> Název <# if ( 'audio' === data.type ) { #> Autor Album <# } #> Titulek{{ data.caption }}Popis{{ data.description }}Nahrál uživatel {{ data.authorName }}<# if ( data.uploadedToTitle ) { #>Přiřazeno <# if ( data.uploadedToLink ) { #> {{ data.uploadedToTitle }} <# } else { #> {{ data.uploadedToTitle }} <# } #><# } #> Kopírovat odkazStránka se zobrazením souboru <# if ( data.can.save ) { #> | Upravit další informace <# } #> <# if ( ! data.uploading && data.can.remove ) { #> |Smazat<# } #><# } else if ( 'image' === data.type && data.sizes ) { #><# } else { #><# if ( data.image && data.image.src && data.image.src !== data.icon ) { #> <# } else if ( data.sizes && data.sizes.medium ) { #> <# } else { #> <# } #>{{ data.filename }}<# } #><# if ( data.buttons.close ) { #>Odstranit<# } #><# if ( data.buttons.check ) { #>Odebrat<# } #> <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; if ( data.describe ) { if ( 'image' === data.type ) { #> <# } else { #> <# if ( 'video' === data.type ) { #> placeholder=\"Stručný popis videa…\" <# } else if ( 'audio' === data.type ) { #> placeholder=\"Stručný popis zvukového souboru…\" <# } else { #> placeholder=\"Stručný popis mediálního souboru…\" <# } #> {{ maybeReadOnly }} /> <# } } #> Uloženo.<# if ( data.uploading ) { #><# } else if ( 'image' === data.type && data.sizes ) { #> <# } else { #> <# } #>{{ data.filename }}{{ data.dateFormatted }}{{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>{{ data.width }} x {{ data.height }} px	<# } #> <# if ( data.can.save && data.sizes ) { #> Upravit obrázek <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>Délka:	{{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( ! data.uploading && data.can.remove ) { #>Smazat<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> Alternativní textPopište smysl obrázku (otevře se na nové záložce). Nevyplňujte jestli je obrázek čistě dekorativní.<# } #> Název <# if ( 'audio' === data.type ) { #> Autor Album <# } #> Titulek{{ data.caption }}Popis{{ data.description }}Kopírovat odkaz <# if ( data.editable ) { #>Upravit výběr<# } #> <# if ( data.clearable ) { #>Vymazat<# } #><# if ( 'image' === data.type ) { #> Zarovnání <# if ( data.userSettings ) { #> data-user-setting=\"align\" <# } #>> Doleva	Na střed	Doprava	Nic	 <# } #><# if ( data.model.canEmbed ) { #> Zobrazení <# } else { #> Odkazovat na <# } #> <# if ( data.userSettings && ! data.model.canEmbed ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( data.model.canEmbed ) { #> Použít mediální přehrávač	<# } else { #> Nic	<# } #> <# if ( data.model.canEmbed ) { #> Odkaz na mediální soubor	<# } else { #> Mediální soubor	<# } #> <# if ( data.model.canEmbed ) { #> Odkaz na stránku se souborem	<# } else { #> Stránku se zobrazením souboru	<# } #> <# if ( 'image' === data.type ) { #> Vlastní URL	<# } #><# if ( 'undefined' !== typeof data.sizes ) { #> Velikost <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Náhled – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Střední – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Velká – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Původní velikost – {{ size.width }} × {{ size.height }} <# } #> <# } #>Odkazovat na <# if ( data.userSettings ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( ! wp.media.galleryDefaults.link || 'post' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Stránku se zobrazením souboru	<# if ( 'file' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Mediální soubor	<# if ( 'none' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> Nic	 Sloupce <# if ( 1 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 1	<# if ( 2 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 2	<# if ( 3 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 3	<# if ( 4 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 4	<# if ( 5 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 5	<# if ( 6 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 6	<# if ( 7 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 7	<# if ( 8 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 8	<# if ( 9 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 9	 Náhodné pořadí Velikost <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #> > Náhled	Střední	Velká	Původní velikost	<# var emptyModel = _.isEmpty( data.model ), isVideo = 'video' === data.controller.get('library').props.get('type'); #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> <# if ( isVideo ) { #> Zobrazit seznam video souborů <# } else { #> Zobrazit seznam audio souborů <# } #> <# if ( ! isVideo ) { #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Zobrazit autora v seznamu souborů <# } #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> Zobrazit náhledový obrázekAlternativní textPopište smysl obrázku (otevře se na nové záložce). Nevyplňujte jestli je obrázek čistě dekorativní.TitulekZarovnáníDoleva	Na střed	Doprava	Nic	Odkazovat naURL obrázku	Vlastní URL	Nic	<# } #>Alternativní textPopište smysl obrázku (otevře se na nové záložce). Nevyplňujte jestli je obrázek čistě dekorativní.Titulek{{ data.model.caption }}Nastavení zobrazení souboruZarovnáníDoleva	Na střed	Doprava	Nic	<# if ( data.attachment ) { #> <# if ( 'undefined' !== typeof data.attachment.sizes ) { #> Velikost <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> Náhled – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> Střední – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> Velká – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> Původní velikost – {{ size.width }} × {{ size.height }} <# } #> Vlastní velikost	 <# } #>Šířka (px) × Výška (px)<# } #>Odkazovat na <# if ( data.attachment ) { #> Mediální soubor	Stránku se zobrazením souboru	<# } else { #> URL obrázku	<# } #> Vlastní URL	Nic	Rozšířené možnostiAtribut title CSS třída (obrázek)<# if ( data.model.linkTargetBlank ) { #>checked=\"checked\"<# } #>>Otevřít odkaz na nové záložceDruh odkazu (rel) Připojit třídu CSS<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #>URLOdstranit audio soubor<# } #> <# if ( ! _.isEmpty( data.model.mp3 ) ) { if ( ! _.isUndefined( html5types.mp3 ) ) { delete html5types.mp3; } #>MP3Odstranit audio soubor<# } #> <# if ( ! _.isEmpty( data.model.ogg ) ) { if ( ! _.isUndefined( html5types.ogg ) ) { delete html5types.ogg; } #>OGGOdstranit audio soubor<# } #> <# if ( ! _.isEmpty( data.model.flac ) ) { if ( ! _.isUndefined( html5types.flac ) ) { delete html5types.flac; } #>FLACOdstranit audio soubor<# } #> <# if ( ! _.isEmpty( data.model.m4a ) ) { if ( ! _.isUndefined( html5types.m4a ) ) { delete html5types.m4a; } #>M4AOdstranit audio soubor<# } #> <# if ( ! _.isEmpty( data.model.wav ) ) { if ( ! _.isUndefined( html5types.wav ) ) { delete html5types.wav; } #>WAVOdstranit audio soubor<# } #> <# if ( ! _.isEmpty( html5types ) ) { #>Přidat další formáty souborů pro přehrávání v prohlížečích, které podporují HTML5:<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #>Předběžně načítatKompletní souborStrukturované informaceNicPřehrát automaticky Opakovat<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #>URLOdstranit video soubor<# } #> <# if ( ! _.isEmpty( data.model.mp4 ) ) { if ( ! _.isUndefined( html5types.mp4 ) ) { delete html5types.mp4; } #>MP4Odstranit video soubor<# } #> <# if ( ! _.isEmpty( data.model.m4v ) ) { if ( ! _.isUndefined( html5types.m4v ) ) { delete html5types.m4v; } #>M4VOdstranit video soubor<# } #> <# if ( ! _.isEmpty( data.model.webm ) ) { if ( ! _.isUndefined( html5types.webm ) ) { delete html5types.webm; } #>WEBMOdstranit video soubor<# } #> <# if ( ! _.isEmpty( data.model.ogv ) ) { if ( ! _.isUndefined( html5types.ogv ) ) { delete html5types.ogv; } #>OGVOdstranit video soubor<# } #> <# if ( ! _.isEmpty( data.model.flv ) ) { if ( ! _.isUndefined( html5types.flv ) ) { delete html5types.flv; } #>FLVOdstranit video soubor<# } #><# if ( ! _.isEmpty( html5types ) ) { #>Přidat další formáty souborů pro přehrávání v prohlížečích, které podporují HTML5:<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #> <# if ( ! _.isEmpty( data.model.poster ) ) { #>Náhledový obrázekOdstranit náhledový obrázek<# } #>Předběžně načítatKompletní souborStrukturované informaceNicPřehrát automaticky Opakovat<# var content = ''; if ( ! _.isEmpty( data.model.content ) ) { var tracks = jQuery( data.model.content ).filter( 'track' ); _.each( tracks.toArray(), function (track) { content += track.outerHTML; #> Textové soubory (titulky, popisky, kapitoly a další informace)Odstranit textový soubor<# } ); #> <# } else { #> Textové soubory (titulky, popisky, kapitoly a další informace) Zatím nebyly přiřazeny žádné položky. <# } #>{{ content }}<# if ( attachment.caption ) { #>{{{ data.verifyHTML( attachment.caption ) }}}<# } #><# if ( index % data.columns === data.columns - 1 ) { #><# } #> <# } ); #><# } else { #>Nebyly nalezeny žádné položky.<# } #>Ikona v prohlížečijob-partnersIkona aplikace v mobilním zařízení"
