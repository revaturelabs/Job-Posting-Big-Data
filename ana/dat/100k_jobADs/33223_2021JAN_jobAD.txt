"诚聘熟手接外卖 | Chinese Business Support关于我们与我们联系我的购物车登陆817 668 1008Toggle navigation国家。U.K siteU.S.A site选择语言中文英文View in EnglishGo to UK site免费为餐饮煤气电报价正在出售的店铺正在出售的店铺卖餐馆或外卖店My Watchlist登卖店广告卖餐馆和外卖店工作工作招聘信用卡借记卡刷卡机， 随时随地收顾客的无接触付款如何登陆卡机程序和让顾客付款817 668 1008按照地区浏览AlabamaArizonaArkansasCaliforniaColoradoConnecticutDelawareFloridaGeorgiaIdahoIllinoisIndianaIowaKansasKentuckyLouisianaMarylandMassachusettsMichiganMinnesotaMississippiMissouriNevadaNew HampshireNew JerseyNew YorkNorth CarolinaNorth DakotaOhioOklahomaOregonPennsylvaniaRhode IslandSouth CarolinaSouth DakotaTennesseeTexasUtahVermontVirginiaWashingtonWisconsin按照类型浏览Buffet商业酒吧住家餐馆超市/商店外卖店关闭菜单	工作» Full time / Part time诚聘熟手接外卖Seafood HouseSalary:面谈信息地图餐馆请人: 请熟手接外卖1名，工资3000另请P/T接外卖1名，法拉盛接送。 联系电话： [Fill in the right box to apply] 或 [Fill in the right box to apply]编号: 11767公布日期: 10-08-2016标签> Full time > Part time > New York申请申请这个工作你的名字你的电子邮件你的电话.你的工作经验输入如上代号申请分享此页...更多工作> New Yorkokinii 诚招前台NY - $面议	詳情诚招日餐前台，熟手优先，会英语。 需要做：日餐打包，收银。 有意者电联： [Fill in 阅读更多请一名熟手日餐企台- $面议	詳情请一名熟手日餐企台，店在长岛，法拉盛接送，有意者可致电 [Fill in the righ 阅读更多纽约albany诚招 外卖打包。N Y - $2500	詳情找一个外卖打包龚，需要手快利落，熟手最好。 包吃住。 有意者请联系林小姐 [Fill in 阅读更多曼哈顿 诚招 打杂兼送外卖。NY - $面议	詳情曼哈顿 诚招 打杂兼送外卖，生手熟手都可以。 另外招厨房助手，要识切菜。 有诚意电话联系： 阅读更多日餐馆诚招企台/铁板师傅NY - $面议	詳情需要熟手有经验。法拉盛接送，包吃。请联系：J [Fill in the right box 阅读更多Chinese Business Support关于我们关于我们与我们联系Terms & ConditionsMy AccountMy Account登卖店招聘广告Unsubscribe正在出售的餐馆和外卖店正在出售的店铺My Watchlist煤气电免费为餐饮煤气电报价登记免费为餐饮煤气电报价卖餐馆或外卖店正在出售的店铺与我们联系Terms & Conditions© 2021, Chinese Business Support, all rights reservedYour IP is 3.239.236.140OK更多信息	关闭这个窗口操作菜单	选择媒体动作	<# } #><# } #><# if ( data.message ) { #>{{ data.message }}<# } #>拖文件至此可上传或选择文件最大上传文件大小：2 MB。	<# if ( data.suggestedWidth && data.suggestedHeight ) { #>建议的图像尺寸：{{data.suggestedWidth}} x {{data.suggestedHeight}}像素。	<# } #>网格视图忽略错误/ –{{ data.message }}<# if ( ! data.hasNext ) { #> disabled<# } #>>编辑下一媒体项目关闭对话框<# if ( data.uploading ) { #><# } else if ( data.sizes && data.sizes.large ) { #> <# } else if ( data.sizes && data.sizes.full ) { #> <# } else if ( -1 === jQuery.inArray( data.type, [ 'audio', 'video' ] ) ) { #> <# } #> <# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } #><# if ( 'image' === data.type && ! data.uploading && data.sizes && data.can.save ) { #>编辑图像<# } else if ( 'pdf' === data.subtype && data.sizes ) { #>文档预览<# } #>已保存。详情文件名： {{ data.filename }}文件类型： {{ data.mime }}上传于： {{ data.dateFormatted }}文件大小： {{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>分辨率： {{ data.width }}×{{ data.height }}像素	<# } #> <# if ( data.originalImageURL && data.originalImageName ) { #> 原始图像：	{{data.originalImageName}} <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>长度： {{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( 'audio' === data.type && data.meta.bitrate ) { #>比特率： {{ Math.round( data.meta.bitrate / 1000 ) }}kb/s <# if ( data.meta.bitrate_mode ) { #> {{ ' ' + data.meta.bitrate_mode.toUpperCase() }} <# } #><# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> 替代文本描述此图像的用途 （在新窗口中打开）。如此图像仅作装饰用，请留空。<# } #> 标题 <# if ( 'audio' === data.type ) { #> 艺术家 相册 <# } #> 说明{{ data.caption }}图像描述{{ data.description }}上传者为 {{ data.authorName }} <# if ( data.uploadedToTitle ) { #> 上传至 <# if ( data.uploadedToLink ) { #> {{ data.uploadedToTitle }} <# } else { #> {{ data.uploadedToTitle }} <# } #> <# } #> 文件URL：复制URL已复制！查看附件页面 <# if ( data.can.save ) { #> | 编辑详细信息 <# } #> <# if ( ! data.uploading && data.can.remove ) { #> |永久删除<# } #><# } else if ( 'image' === data.type && data.sizes ) { #><# } else { #><# if ( data.image && data.image.src && data.image.src !== data.icon ) { #> <# } else if ( data.sizes && data.sizes.medium ) { #> <# } else { #> <# } #>{{ data.filename }}<# } #><# if ( data.buttons.close ) { #>移除<# } #><# if ( data.buttons.check ) { #>取消选择<# } #> <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; if ( data.describe ) { if ( 'image' === data.type ) { #> <# } else { #> <# if ( 'video' === data.type ) { #> aria-label=\"视频标题\" placeholder=\"视频标题…\" <# } else if ( 'audio' === data.type ) { #> aria-label=\"音频标题\" placeholder=\"音频标题…\" <# } else { #> aria-label=\"媒体标题\" placeholder=\"媒体标题…\" <# } #> {{ maybeReadOnly }} /> <# } } #> 已保存。<# if ( data.uploading ) { #><# } else if ( 'image' === data.type && data.sizes ) { #> <# } else { #> <# } #>{{ data.filename }}{{ data.dateFormatted }}{{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>{{ data.width }}×{{ data.height }}像素	<# } #> <# if ( data.originalImageURL && data.originalImageName ) { #> 原始图像：	{{data.originalImageName}} <# } #> <# if ( data.can.save && data.sizes ) { #> 编辑图像 <# } #> <# } #> <# if ( data.fileLength && data.fileLengthHumanReadable ) { #>长度：	{{ data.fileLength }} {{ data.fileLengthHumanReadable }}<# } #> <# if ( ! data.uploading && data.can.remove ) { #>永久删除<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #><# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> <# if ( 'image' === data.type ) { #> 替代文本描述此图像的用途 （在新窗口中打开）。如此图像仅作装饰用，请留空。<# } #> 标题 <# if ( 'audio' === data.type ) { #> 艺术家 相册 <# } #> 说明{{ data.caption }}图像描述{{ data.description }}文件URL：复制URL已复制！<# if ( data.editable ) { #>编辑所选<# } #> <# if ( data.clearable ) { #>清空<# } #><# if ( 'image' === data.type ) { #> 对齐方式 <# if ( data.userSettings ) { #> data-user-setting=\"align\" <# } #>> 左	中	右	无	 <# } #> <# if ( data.model.canEmbed ) { #> 嵌入或链接	<# } else { #> 链接到	<# } #> <# if ( data.userSettings && ! data.model.canEmbed ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( data.model.canEmbed ) { #> 内嵌媒体播放器	<# } else { #> 无	<# } #> <# if ( data.model.canEmbed ) { #> 链接到媒体文件	<# } else { #> 媒体文件	<# } #> <# if ( data.model.canEmbed ) { #> 链接到附件页面	<# } else { #> 附件页面	<# } #> <# if ( 'image' === data.type ) { #> 自定义URL	<# } #> URL <# if ( 'undefined' !== typeof data.sizes ) { #> 尺寸 <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> 缩略图 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> 中等 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> 大 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> 完整尺寸 – {{ size.width }} × {{ size.height }} <# } #> <# } #>链接到 <# if ( data.userSettings ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( ! wp.media.galleryDefaults.link || 'post' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> 附件页面	<# if ( 'file' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> 媒体文件	<# if ( 'none' === wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> 无	 栏目 <# if ( 1 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 1	<# if ( 2 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 2	<# if ( 3 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 3	<# if ( 4 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 4	<# if ( 5 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 5	<# if ( 6 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 6	<# if ( 7 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 7	<# if ( 8 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 8	<# if ( 9 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 9	 随机顺序 尺寸 <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #> > 缩略图	中等	大	完整尺寸	<# var emptyModel = _.isEmpty( data.model ), isVideo = 'video' === data.controller.get('library').props.get('type'); #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> <# if ( isVideo ) { #> 显示视频列表	<# } else { #> 显示曲目列表	<# } #> <# if ( ! isVideo ) { #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> 在曲目列表中显示艺术家名	 <# } #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> 显示图像	替代文本描述此图像的用途 （在新窗口中打开）。如此图像仅作装饰用，请留空。说明对齐左	中	右	无	链接到图像URL	自定义URL	无	URL描述此图像的用途 （在新窗口中打开）。如此图像仅作装饰用，请留空。说明{{ data.model.caption }}显示设置对齐左	中	右	无	<# if ( data.attachment ) { #> <# if ( 'undefined' !== typeof data.attachment.sizes ) { #> 尺寸 <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> 缩略图 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> 中等 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> 大 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> 完整尺寸 – {{ size.width }} × {{ size.height }} <# } #> 自定义尺寸	 <# } #>宽度 × 高度图像的像素尺寸<# } #> 链接到 <# if ( data.attachment ) { #> 媒体文件	附件页面	<# } else { #> 图像URL	<# } #> 自定义URL	无	 URL高级选项图像标题属性 图像CSS类<# if ( data.model.linkTargetBlank ) { #>checked=\"checked\"<# } #>> 在新标签页中打开链接 链接Rel 链接CSS类<# if ( data.attachment && window.imageEdit ) { #><# } #><# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> URL移除音频来源<# } #> <# if ( ! _.isEmpty( data.model.mp3 ) ) { if ( ! _.isUndefined( html5types.mp3 ) ) { delete html5types.mp3; } #> MP3移除音频来源<# } #> <# if ( ! _.isEmpty( data.model.ogg ) ) { if ( ! _.isUndefined( html5types.ogg ) ) { delete html5types.ogg; } #> OGG移除音频来源<# } #> <# if ( ! _.isEmpty( data.model.flac ) ) { if ( ! _.isUndefined( html5types.flac ) ) { delete html5types.flac; } #> FLAC移除音频来源<# } #> <# if ( ! _.isEmpty( data.model.m4a ) ) { if ( ! _.isUndefined( html5types.m4a ) ) { delete html5types.m4a; } #> M4A移除音频来源<# } #> <# if ( ! _.isEmpty( data.model.wav ) ) { if ( ! _.isUndefined( html5types.wav ) ) { delete html5types.wav; } #> WAV移除音频来源<# } #> <# if ( ! _.isEmpty( html5types ) ) { #>添加备用源以增强HTML5播放体验 <# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #>预加载自动元数据无自动播放 循环<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> URL移除视频来源<# } #> <# if ( ! _.isEmpty( data.model.mp4 ) ) { if ( ! _.isUndefined( html5types.mp4 ) ) { delete html5types.mp4; } #> MP4移除视频来源<# } #> <# if ( ! _.isEmpty( data.model.m4v ) ) { if ( ! _.isUndefined( html5types.m4v ) ) { delete html5types.m4v; } #> M4V移除视频来源<# } #> <# if ( ! _.isEmpty( data.model.webm ) ) { if ( ! _.isUndefined( html5types.webm ) ) { delete html5types.webm; } #> WEBM移除视频来源<# } #> <# if ( ! _.isEmpty( data.model.ogv ) ) { if ( ! _.isUndefined( html5types.ogv ) ) { delete html5types.ogv; } #> OGV移除视频来源<# } #> <# if ( ! _.isEmpty( data.model.flv ) ) { if ( ! _.isUndefined( html5types.flv ) ) { delete html5types.flv; } #> FLV移除视频来源<# } #><# if ( ! _.isEmpty( html5types ) ) { #>添加备用源以增强HTML5播放体验 <# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #> <# if ( ! _.isEmpty( data.model.poster ) ) { #> 海报图像移除海报图像<# } #>预加载自动元数据无自动播放 循环 <# var content = ''; if ( ! _.isEmpty( data.model.content ) ) { var tracks = jQuery( data.model.content ).filter( 'track' ); _.each( tracks.toArray(), function( track, index ) { content += track.outerHTML; #> 曲目（字幕、说明、描述、章节或元数据） srclang、label和kind可被编辑并设置为视频的语言和种类。	移除视频轨道<# } ); #> <# } else { #> 曲目（字幕、说明、描述、章节或元数据）没有关联的字幕。 <# } #>{{ content }}<# if ( attachment.caption ) { #>{{{ data.verifyHTML( attachment.caption ) }}}<# } #><# if ( index % data.columns === data.columns - 1 ) { #><# } #> <# } ); #><# } else { #>找不到条目。<# } #>作为浏览器图标<# print( 'Chinese Business Support' ) #>作为app图标首页煤气电商用工作账号发布"
