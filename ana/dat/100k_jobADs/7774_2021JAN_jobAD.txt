"本サイト、「アピール」とは | アピール | 未来の教育を作る人のマガジンレアジョブについて働くひと新しい教育採用情報はこちらレアジョブについて本サイト、「アピール」とはレアジョブについてピックアップ2016/09/23 2018/07/25ツイートシェアはてブ本サイト、“アピール” とは、株式会社レアジョブが運営するサイトです。レアジョブ内部の人へのインタビューを中心に、教育・海外・組織・ベンチャー 等々、様々なテーマの話を掲載します。レアジョブ社員の話を聞くにあたっては、「同じ会社の人である」というバイアスが入ってしまわないよう、外部の方にインタビュアーをお願いし、可能な限り客観的な記事を作成するよう務めております。専属インタビュアー聴き手インタビューをする為に雇われた、外部の人。レアジョブについてピックアップツイートシェアはてブRECOMMEND2017/07/12お客様の英語学習に寄り添い、伴走する！英会話のパーソナルトレーナーを募集中	2019/07/22オンライン英会話サービスが拓いてきた “グローバルで活躍する”という価値…	2018/03/28限られた裁量の中で新規プロダクトを最短リリースした、という話	2018/03/23\"個々が強みを活かす\"カスタマーサポートのチーム作りで工夫していること	2016/09/22レアジョブのこと、 今日は本音で語り合ってみよう	2017/06/30図と数字で見るレアジョブ　サービス篇	2017/07/31“Skypeオンライン英会話”にたどり着くまでの紆余曲折　—レアジョブ社…	2017/01/06レアジョブは、レアジョブ社員の英語力向上に努めています。	カテゴリーレアジョブについて働くひと新しい教育タグイベント インターンシップ エンジニア カスタマーサポート コーポレート デザイナー ピックアップ プロダクト企画 リーダーの履歴書 出来事 営業・マーケティング 外部記事 挑戦する 海外で働く 社会を良くする 社内制度・文化 英語学習サポートエンジニアブログ©Copyright2021 アピール | 未来の教育を作る人のマガジン.All Rights Reserved.<# } #><# if ( data.message ) { #>{{ data.message }}<# } #>アップロードするファイルをドロップまたはファイルを選択最大アップロードサイズ: 0 B。<# if ( data.suggestedWidth && data.suggestedHeight ) { #>推奨画像サイズ: {{data.suggestedWidth}}×{{data.suggestedHeight}}ピクセル。	<# } #>グリッド表示エラーを非表示/ –{{ data.message }}次のメディア項目を編集<# } else if ( data.sizes && data.sizes.large ) { #> <# } else if ( data.sizes && data.sizes.full ) { #> <# } else if ( -1 === jQuery.inArray( data.type, [ 'audio', 'video' ] ) ) { #> <# } #> <# if ( 'audio' === data.type ) { #><# } else if ( 'video' === data.type ) { var w_rule = ''; if ( data.width ) { w_rule = 'width: ' + data.width + 'px;'; } else if ( wp.media.view.settings.contentWidth ) { w_rule = 'width: ' + wp.media.view.settings.contentWidth + 'px;'; } #><# if ( data.width ) { #>width=\"{{ data.width }}\"<# } #> <# if ( data.height ) { #>height=\"{{ data.height }}\"<# } #> <# if ( data.image && data.image.src !== data.icon ) { #>poster=\"{{ data.image.src }}\"<# } #>><# } #><# if ( 'image' === data.type && ! data.uploading && data.sizes && data.can.save ) { #>画像を編集<# } else if ( 'pdf' === data.subtype && data.sizes ) { #> ドキュメントプレビュー	<# } #>保存しました。ファイル名: {{ data.filename }}ファイルタイプ: {{ data.mime }}更新日: {{ data.dateFormatted }}ファイルサイズ: {{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>サイズ: {{ data.width }} × {{ data.height }}<# } #> <# } #> <# if ( data.fileLength ) { #>長さ: {{ data.fileLength }}<# } #> <# if ( 'audio' === data.type && data.meta.bitrate ) { #>ビットレート: {{ Math.round( data.meta.bitrate / 1000 ) }}kb/s <# if ( data.meta.bitrate_mode ) { #> {{ ' ' + data.meta.bitrate_mode.toUpperCase() }} <# } #><# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #>URL <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> タイトル <# if ( 'audio' === data.type ) { #> アーティスト アルバム <# } #> キャプション{{ data.caption }}<# if ( 'image' === data.type ) { #> 代替テキスト <# } #> 説明{{ data.description }}アップロード: {{ data.authorName }} <# if ( data.uploadedToTitle ) { #> アップロード先: <# if ( data.uploadedToLink ) { #> {{ data.uploadedToTitle }} <# } else { #> {{ data.uploadedToTitle }} <# } #> <# } #>添付ファイルのページを表示 <# if ( data.can.save ) { #> | さらに詳細を編集 <# } #> <# if ( ! data.uploading && data.can.remove ) { #> |完全に削除する<# } #><# } else if ( 'image' === data.type && data.sizes ) { #><# } else { #><# if ( data.image && data.image.src && data.image.src !== data.icon ) { #> <# } else if ( data.sizes && data.sizes.medium ) { #> <# } else { #> <# } #>{{ data.filename }}<# } #><# if ( data.buttons.close ) { #>削除<# } #><# if ( data.buttons.check ) { #>選択を解除<# } #> <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; if ( data.describe ) { if ( 'image' === data.type ) { #> <# } else { #> <# if ( 'video' === data.type ) { #> placeholder=\"このビデオの説明を書く\" <# } else if ( 'audio' === data.type ) { #> placeholder=\"この音声の説明を書く\" <# } else { #> placeholder=\"このメディアの説明を書く\" <# } #> {{ maybeReadOnly }} /> <# } } #> 保存しました。<# if ( data.uploading ) { #><# } else if ( 'image' === data.type && data.sizes ) { #> <# } else { #> <# } #>{{ data.filename }}{{ data.dateFormatted }}{{ data.filesizeHumanReadable }}<# if ( 'image' === data.type && ! data.uploading ) { #> <# if ( data.width && data.height ) { #>{{ data.width }} × {{ data.height }}<# } #> <# if ( data.can.save && data.sizes ) { #> 画像を編集 <# } #> <# } #> <# if ( data.fileLength ) { #>長さ: {{ data.fileLength }}<# } #> <# if ( ! data.uploading && data.can.remove ) { #>完全に削除する<# } #><# if ( data.compat && data.compat.meta ) { #> {{{ data.compat.meta }}} <# } #>URL <# var maybeReadOnly = data.can.save || data.allowLocalEdits ? '' : 'readonly'; #> タイトル <# if ( 'audio' === data.type ) { #> アーティスト アルバム <# } #> キャプション{{ data.caption }}<# if ( 'image' === data.type ) { #> 代替テキスト <# } #> 説明{{ data.description }}<# if ( data.editable ) { #>選択範囲を編集<# } #> <# if ( data.clearable ) { #>クリア<# } #><# if ( 'image' === data.type ) { #> 配置 <# if ( data.userSettings ) { #> data-user-setting=\"align\" <# } #>> 左	中央	右	なし	 <# } #><# if ( data.model.canEmbed ) { #> 埋め込みまたはリンク <# } else { #> リンク先 <# } #> <# if ( data.userSettings && ! data.model.canEmbed ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( data.model.canEmbed ) { #> メディアプレイヤーを埋め込む	<# } else { #> なし	<# } #> <# if ( data.model.canEmbed ) { #> メディアファイルへのリンク	<# } else { #> メディアファイル	<# } #> <# if ( data.model.canEmbed ) { #> 添付ファイルページへのリンク	<# } else { #> 添付ファイルのページ	<# } #> <# if ( 'image' === data.type ) { #> カスタム URL	<# } #><# if ( 'undefined' !== typeof data.sizes ) { #> サイズ <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> サムネイル – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> 中 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> 大 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> フルサイズ – {{ size.width }} × {{ size.height }} <# } #> <# } #>リンク先 <# if ( data.userSettings ) { #> data-user-setting=\"urlbutton\" <# } #>> <# if ( ! wp.media.galleryDefaults.link || 'post' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> 添付ファイルのページ	<# if ( 'file' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> メディアファイル	<# if ( 'none' == wp.media.galleryDefaults.link ) { #>selected=\"selected\"<# } #>> なし	 カラム <# if ( 1 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 1	<# if ( 2 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 2	<# if ( 3 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 3	<# if ( 4 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 4	<# if ( 5 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 5	<# if ( 6 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 6	<# if ( 7 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 7	<# if ( 8 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 8	<# if ( 9 == wp.media.galleryDefaults.columns ) { #>selected=\"selected\"<# } #>> 9	 ランダム サイズ <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #> > サムネイル	中	大	フルサイズ	<# var emptyModel = _.isEmpty( data.model ), isVideo = 'video' === data.controller.get('library').props.get('type'); #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> <# if ( isVideo ) { #> 動画一覧を表示 <# } else { #> トラックリストを表示 <# } #> <# if ( ! isVideo ) { #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> トラックリストにアーティスト名を表示 <# } #> <# if ( emptyModel ) { #> checked=\"checked\" <# } #> /> 画像を表示キャプション代替テキスト配置左	中央	右	なし	リンク先画像の URL	カスタム URL	なし	<# } #>キャプション{{ data.model.caption }}代替テキスト設定を表示配置左	中央	右	なし	<# if ( data.attachment ) { #> <# if ( 'undefined' !== typeof data.attachment.sizes ) { #> サイズ <# if ( data.userSettings ) { #> data-user-setting=\"imgsize\" <# } #>> <# var size = data.sizes['thumbnail']; if ( size ) { #> サムネイル – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['medium']; if ( size ) { #> 中 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['large']; if ( size ) { #> 大 – {{ size.width }} × {{ size.height }} <# } #> <# var size = data.sizes['full']; if ( size ) { #> フルサイズ – {{ size.width }} × {{ size.height }} <# } #> カスタムサイズ	 <# } #>幅 (px) × 高さ (px)<# } #>リンク先 <# if ( data.attachment ) { #> メディアファイル	添付ファイルのページ	<# } else { #> 画像の URL	<# } #> カスタム URL	なし	上級者向け設定画像タイトル属性 画像 CSS クラス<# if ( data.model.linkTargetBlank ) { #>checked=\"checked\"<# } #>>リンクを新しいタブで開くリンク rel 属性 リンク CSS クラス<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> SRC音声ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.mp3 ) ) { if ( ! _.isUndefined( html5types.mp3 ) ) { delete html5types.mp3; } #> MP3音声ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.ogg ) ) { if ( ! _.isUndefined( html5types.ogg ) ) { delete html5types.ogg; } #> OGG音声ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.m4a ) ) { if ( ! _.isUndefined( html5types.m4a ) ) { delete html5types.m4a; } #> M4A音声ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.wav ) ) { if ( ! _.isUndefined( html5types.wav ) ) { delete html5types.wav; } #> WAV音声ソースを削除<# } #> <# if ( ! _.isEmpty( html5types ) ) { #>フルスクリーン再生のための別ソースを追加<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #>先読み自動メタデータなし自動再生 ループ<# if ( ! _.isEmpty( data.model.src ) ) { ext = data.model.src.split('.').pop(); if ( html5types[ ext ] ) { delete html5types[ ext ]; } #> SRC映像ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.mp4 ) ) { if ( ! _.isUndefined( html5types.mp4 ) ) { delete html5types.mp4; } #> MP4映像ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.m4v ) ) { if ( ! _.isUndefined( html5types.m4v ) ) { delete html5types.m4v; } #> M4V映像ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.webm ) ) { if ( ! _.isUndefined( html5types.webm ) ) { delete html5types.webm; } #> WEBM映像ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.ogv ) ) { if ( ! _.isUndefined( html5types.ogv ) ) { delete html5types.ogv; } #> OGV映像ソースを削除<# } #> <# if ( ! _.isEmpty( data.model.flv ) ) { if ( ! _.isUndefined( html5types.flv ) ) { delete html5types.flv; } #> FLV映像ソースを削除<# } #><# if ( ! _.isEmpty( html5types ) ) { #>フルスクリーン再生のための別ソースを追加<# _.each( html5types, function (mime, type) { #>{{ type }}<# } ) #><# } #> <# if ( ! _.isEmpty( data.model.poster ) ) { #> ポスター画像ポスター画像を削除<# } #>先読み自動メタデータなし自動再生 ループ トラック (字幕、キャプション、説明、チャプター、またはメタ情報) <# var content = ''; if ( ! _.isEmpty( data.model.content ) ) { var tracks = jQuery( data.model.content ).filter( 'track' ); _.each( tracks.toArray(), function (track) { content += track.outerHTML; #>動画トラックを削除<# } ); #> <# } else { #> 関連付けられた字幕がありません。 <# } #>{{ content }}<# if ( attachment.caption ) { #>{{{ data.verifyHTML( attachment.caption ) }}}<# } #><# if ( index % data.columns === data.columns - 1 ) { #><# } #> <# } ); #><# } else { #>項目は見つかりませんでした。<# } #>ブラウザーアイコンとしてアピール | 未来の教育を作る人のマガジンアプリアイコンとして"
